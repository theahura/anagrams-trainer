# games

Browser games collected in one repo.

- `index.html` is the games index page.
- `games/reword/` contains the Reword source code.
- `reword/` is the standalone entry page for the published Reword game.
